# Deploy Report: User Bookings List

> Feature slug: `user-bookings-list`
> Date: 2026-05-21
> Environment: `dev/staging`

---

## 1) Quality Gates
| Check | Status | Notes |
|---|---|---|
| lint | `PASS` | 0 errors, 10 warnings (warnings are outside of feature scope) |
| typecheck | `PASS` | 0 errors |
| check:routes | `PASS` | Route check passed (Verified 18 active route entries) |
| build | `PASS` | Production build succeeded with Webpack optimization |
| prepush:check | `PASS` | Full pre-push quality gate completed successfully |

## 1.1) Build Notes
- **Build Commands Run:** `npm run prepush:check` which internally triggers `eslint`, `tsc --noEmit`, `node scripts/check-routes.mjs`, and `next build --webpack`.
- **Build Warnings:** 
  - Next.js: `middleware` file convention is deprecated in favor of `proxy`.
  - Next.js: Warning regarding experimental edge runtime usage.
  - ESLint: 10 warnings in unrelated files (`src/app/[locale]/(main)/(protected)/tours/[slug]/book/page.tsx`, textareas, etc.).
- **Follow-up:** None. The bundle compiles cleanly and routes are verified.

## 2) Cloudflare Build / Deploy Status
| Step | Status | Notes |
|---|---|---|
| build:cloudflare | `SKIPPED` | Cloudflare-specific bundle commands not executed locally |
| preview:cloudflare | `SKIPPED` | No local Wrangler preview |
| deploy:cloudflare | `PENDING` | Automated deployment via GitHub Actions CI/CD to Cloudflare Pages |

*Note: The project is set up to build and deploy to Cloudflare Pages automatically via CI pipelines upon branch merger.*

## 3) Smoke Test
| Scenario | Status | Notes |
|---|---|---|
| page load | `SKIPPED` | Local development server was offline during execution |
| critical flow | `SKIPPED` | Booking list navigation and cancellation flow unverified at runtime |
| locale switch | `PASS` | Static translation files (`vi/en`) are fully synchronized |
| auth redirect | `PASS` | Middleware configuration verified (`/bookings` redirects to login) |
| browser console | `SKIPPED` | Browser error checks skipped |

## 3.1) Additional Scenarios
| Scenario | Status | Notes |
|---|---|---|
| empty state | `PASS` | Static review of `EmptyState` component wiring is correct |
| error state | `PASS` | Retry logic and error banner properly wired to TanStack Query `refetch` |
| mobile responsive | `SKIPPED` | Visual layout scaling unverified in live viewports |

## 4) Deploy Readiness
- **Verdict:** `READY WITH RISKS`
- **Risks & Mitigation:**
  - *Risk:* No runtime verification (browser smoke testing, visual review, responsiveness, console exceptions) due to dev server being offline.
  - *Mitigation:* Ensure pre-deployment staging validation is performed by the reviewer or once a preview URL is generated by the CI/CD pipeline on Cloudflare.

## 5) Evidence / References
- **Test Report:** [2026-05-21__user-bookings-list__test-report.md](file:///D:/DATN/danangtrip-web/.agent/artifacts/test-cases/2026-05-21__user-bookings-list__test-report.md)
- **Review Report:** [2026-05-21__user-bookings-list__review.md](file:///D:/DATN/danangtrip-web/.agent/artifacts/review/2026-05-21__user-bookings-list__review.md)
